<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>PO PDF Tools</title>
  <style>
    * { margin: 0; padding: 0; box-sizing: border-box; }
    body {
      font-family: Arial, sans-serif;
      background-color: #121212;
      color: #e0e0e0;
      padding: 10px;
    }
    h1 {
      text-align: center;
      font-size: 5rem;
      margin: 20px 0;
      color: #ffffff;
    }
    .accordion {
      max-width: 100%;
      margin: 0 auto;
    }
    details {
      background-color: #1e1e1e;
      border: 1px solid #333;
      margin-bottom: 10px;
      border-radius: 8px;
      overflow: hidden;
    }
    summary {
      padding: 20px;
      font-size: 4.5rem;
      font-weight: bold;
      cursor: pointer;
      background-color: #0d47a1;
      color: #ffffff;
      display: flex;
      justify-content: space-between;
      align-items: center;
      user-select: none;
    }
    summary::after {
      content: '\25BC';
      font-size: 1em;
      transition: transform 0.3s ease;
    }
    details[open] summary::after {
      transform: rotate(180deg);
    }
    .panel {
      padding: 20px;
      background-color: #212121;
      border-top: 1px solid #333;
    }
    .remote-btn {
      width: 100%;
      padding: 25px;
      font-size: 3.5rem;
      border: none;
      border-radius: 6px;
      background: #333;
      color: #fff;
      cursor: pointer;
      margin-bottom: 10px;
      transition: background 0.2s ease, transform 0.1s ease;
    }
    .remote-btn:hover { background: #444; }
    .remote-btn:active { transform: scale(0.98); }
    .pdf { background: #6f42c1; }
    .emailprep { background: #009688; }
    .purge { background: #e63946; }
    .menards { background: #4caf50; }
    .wcs { background: #ff9800; } 
    .pdf:hover { background: #7f56d9; }
    .emailprep:hover { background: #00796b; }
    .purge:hover { background: #c53030; }
    .menards:hover { background: #43a047; }
    .wcs:hover { background: #f57c00; }

    /* --- UPDATED TOAST CSS --- */
    #toast {
      position: fixed;
      bottom: 20px;
      left: 10px;  /* Spans the width on mobile */
      right: 10px;
      background-color: #333;
      color: white;
      padding: 20px 24px;   /* Increased padding */
      border-radius: 8px;
      display: none;
      z-index: 1000;
      font-size: 2.0rem;    /* Significantly larger font */
      text-align: center;
      box-shadow: 0 4px 10px rgba(0,0,0,0.5); /* Added shadow for visibility */
    }

    /* On larger screens, move it back to the corner */
    @media (min-width: 768px) {
      #toast {
        left: auto; /* Reset left */
        right: 20px;
        bottom: 20px;
        max-width: 450px; /* Give it a max width */
        text-align: left; /* Reset text alignment */
      }
    }
    /* --- END UPDATED CSS --- */

    @media (hover: none) {
      summary { padding: 25px; }
    }

/* --- Custom Confirm Modal CSS --- */
#confirmOverlay {
  position: fixed;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  background: rgba(0, 0, 0, 0.7);
  z-index: 2000;
  display: none; /* Hidden by default */
  align-items: center;
  justify-content: center;
}

#confirmBox {
  background: #1e1e1e;
  padding: 24px;
  border-radius: 8px;
  border: 1px solid #333;
  box-shadow: 0 5px 15px rgba(0,0,0,0.5);
  width: 90%;
  max-width: 500px;
}

#confirmMessage {
  font-size: 2.5rem; /* Large font */
  color: #e0e0e0;
  margin-bottom: 20px;
  line-height: 1.4;
  text-align: center;
}

#confirmButtons {
  display: flex;
  justify-content: space-around;
  gap: 15px;
}

#confirmButtons .remote-btn {
   width: 45%; /* Buttons share the space */
   margin-bottom: 0; /* Remove default margin */
   font-size: 2.5rem; /* Match message */
}

/* Style the "No" button to be less prominent */
#confirmBtnNo {
  background: #555;
}
#confirmBtnNo:hover {
  background: #666;
}
/* --- End Custom Modal CSS --- */



  </style>
</head>
<body>
  <h1>PO PDF Tools</h1>
  <div class="accordion">

    <details>
      <summary>PDF Operations</summary>
      <div class="panel">

        <details>
          <summary>Daily File Renaming</summary>
          <div class="panel">
            <button class="remote-btn pdf" onclick="runFunction('renameTradingPartnerFiles')">Rename Trading Partner Packing Slips</button>
            <button class="remote-btn pdf" onclick="runFunction('renameShippingLabels')">Rename Shipping Labels</button>
            <button class="remote-btn pdf" onclick="runFunction('renameShippingManifestPDFs')">Rename Shipping Manifests</button>
            <button class="remote-btn pdf" onclick="runFunction('renameFileToPDF')">Rename Files With No Extension to PDF</button>
            
          </div>
        </details>

        <details>
          <summary>Friday File Renaming</summary>
          <div class="panel">
            <details>
              <summary>Renaming (-thurs)</summary>
              <div class="panel">
                <button class="remote-btn pdf" onclick="runFunction('renameTradingPartnerFilesThurs')">Rename Trading Partner Packing Slips (-thurs)</button>
                <button class="remote-btn pdf" onclick="runFunction('renameShippingLabelsThurs')">Rename Shipping Labels (-thurs)</button>
                <button class="remote-btn pdf" onclick="runFunction('renameShippingManifestPDFsThurs')">Rename Shipping Manifests (-thurs)</button>
              </div>
            </details>
          </div>
        </details>
        
        <details>
          <summary>Sunday File Renaming</summary>
          <div class="panel">
            <details>
              <summary>Renaming (-sun)</summary>
              <div class="panel">
                <button class="remote-btn pdf" onclick="runFunction('renameTradingPartnerFilesSun')">Rename Trading Partner Packing Slips (-sun)</button>
                <button class="remote-btn pdf" onclick="runFunction('renameShippingLabelsSun')">Rename Shipping Labels (-sun)</button>
                <button class="remote-btn pdf" onclick="runFunction('renameShippingManifestPDFsSun')">Rename Shipping Manifests (-sun)</button>
              </div>
            </details>
          </div>
        </details>

        <details>
          <summary>Copying</summary>
          <div class="panel">
            <button class="remote-btn pdf" onclick="runFunction('copyTradingPartnerPDFs')">Copy Trading Partner Packing Slips</button>
            <button class="remote-btn pdf" onclick="runFunction('copyShippingLabelPDFs')">Copy Shipping Labels</button>
            <button class="remote-btn pdf" onclick="runFunction('copyShippingManifestPDFs')">Copy Shipping Manifests</button>
          </div>
        </details>

        <details>
          <summary>Prepping</summary>
          <div class="panel">
            <button class="remote-btn pdf" onclick="runFunction('emailPageCountReport')">Generate Pack Count</button>
            <button class="remote-btn pdf" onclick="runFunction('mergeManifests')">Merge Manifests</button>
            <button class="remote-btn pdf" onclick="runFunction('moveShippingDocuments')">Move Shipping Documents To Email Folder</button>
            <button class="remote-btn emailprep" onclick="runFunction('prepareShippingDocs')">Run Email Prep</button>
          </div>
        </details>

      </div>
    </details>

    <details>
      <summary>Purge Operations</summary>
      <div class="panel">
        <button class="remote-btn purge" onclick="runFunction('purgeDailyShippingDocs')">Purge Daily Shipping Doc Folder</button>
        <button class="remote-btn purge" onclick="runFunction('purgeAllPDFs')">Purge All PDFs</button>
      </div>
    </details>

  </div>

  <div id="toast"></div>

<script>
    // --- NEW LINE ---
    // This tells the script to wait until the HTML page is fully loaded
    document.addEventListener('DOMContentLoaded', () => {

      // --- Custom Confirm Modal Logic ---
      // Now these are guaranteed to be found!
      const confirmOverlay = document.getElementById('confirmOverlay');
      const confirmMessageEl = document.getElementById('confirmMessage');
      const confirmBtnYes = document.getElementById('confirmBtnYes');
      const confirmBtnNo = document.getElementById('confirmBtnNo');

      /**
       * Shows a custom, styleable confirm dialog.
       * @param {string} message The message to display.
       * @returns {Promise<boolean>} Resolves true for "Yes", false for "No".
       */
      function customConfirm(message) {
        confirmMessageEl.textContent = message;
        confirmOverlay.style.display = 'flex';

        return new Promise((resolve) => {
          confirmBtnYes.addEventListener('click', () => {
            confirmOverlay.style.display = 'none';
            resolve(true); // User clicked "Yes"
          }, { once: true });

          confirmBtnNo.addEventListener('click', () => {
            confirmOverlay.style.display = 'none';
            resolve(false); // User clicked "No"
          }, { once: true });
        });
      }
      // --- End Custom Confirm ---


      function showToast(message) {
        const toast = document.getElementById('toast');
        toast.textContent = message;
        toast.style.display = 'block';
        setTimeout(() => { toast.style.display = 'none'; }, 3000);
      }

      // Your custom toast messages
      const toastMessages = {
        'renameTradingPartnerFiles': 'Renaming Partner Slips...',
        'renameShippingLabels': 'Renaming Shipping Labels...',
        'renameShippingManifestPDFs': 'Renaming Manifests...',
        'renameFileToPDF': 'Fixing file extensions...',
        'renameTradingPartnerFilesThurs': 'Renaming Partner Slips (Thurs)...',
        'renameShippingLabelsThurs': 'Renaming Labels (Thurs)...',
        'renameShippingManifestPDFsThurs': 'Renaming Manifests (Thurs)...',
        'renameTradingPartnerFilesSun': 'Renaming Partner Slips (Sun)...',
        'renameShippingLabelsSun': 'Renaming Labels (Sun)...',
        'renameShippingManifestPDFsSun': 'Renaming Manifests (Sun)...',
        'copyTradingPartnerPDFs': 'Copying Partner Slips...',
        'copyShippingLabelPDFs': 'Copying Shipping Labels...',
        'copyShippingManifestPDFs': 'Copying Manifests...',
        'emailPageCountReport': 'Generating Pack Count Report...',
        'mergeManifests': 'Merging Manifests... 🖇️',
        'moveShippingDocuments': 'Moving Docs to Email Folder...',
        'prepareShippingDocs': 'Running Email Prep... 📧',
        'purgeDailyShippingDocs': 'Purging Daily Docs... 🗑️',
        'purgeAllPDFs': 'PURGING ALL PDFs... ⚠️'
      };

      // --- UPDATED runFunction ---
      // We must attach this function to the global 'window' object
      // so the 'onclick=""' in the HTML can find it inside the listener.
      window.runFunction = async function(funcName) {
        
        if (funcName === 'purgeDailyShippingDocs' || funcName === 'purgeAllPDFs') {
          
          let confirmMessage = 'Are you sure?';
          if (funcName === 'purgeDailyShippingDocs') {
            confirmMessage = 'ARE YOU SURE you want to purge the Daily Shipping Doc Folder?';
          } else if (funcName === 'purgeAllPDFs') {
            confirmMessage = 'DANGER: ARE YOU SURE you want to purge ALL PDFs? This cannot be undone.';
          }

          const confirmed = await customConfirm(confirmMessage);
          
          if (!confirmed) {
            showToast('Purge cancelled.');
            return; 
          }
        }
        
        const defaultMessage = `Running ${funcName}...`;
        const initialMessage = toastMessages[funcName] || defaultMessage;

        showToast(initialMessage); 
        
        google.script.run
          .withSuccessHandler(result => showToast(result)) 
          .withFailureHandler(error => showToast(`Error in ${funcName}: ${error.message}`))
          [funcName]();
      }

    // --- NEW LINE ---
    // Closes the DOMContentLoaded listener
    });
  </script>






  <div id="confirmOverlay">
  <div id="confirmBox">
    <div id="confirmMessage"></div>
    <div id="confirmButtons">
      <button id="confirmBtnYes" class="remote-btn purge">Yes</button>
      <button id="confirmBtnNo" class="remote-btn">No</button>
    </div>
  </div>
</div>
</body>
</html>
