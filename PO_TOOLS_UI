<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>PO PDF Tools</title>

  <style>
    /* Reset some defaults for better mobile experience */
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      font-family: Arial, sans-serif;
      background-color: #121212; /* Dark background */
      color: #e0e0e0; /* Light text for contrast */
      padding: 10px;
    }

    h1 {
      text-align: center;
      font-size: 5rem;
      margin: 20px 0;
      color: #ffffff;
    }

    /* Accordion container */
    .accordion {
      max-width: 100%;
      margin: 0 auto;
    }

    /* Style the accordion items */
    details {
      background-color: #1e1e1e;
      border: 1px solid #333;
      margin-bottom: 10px;
      border-radius: 8px;
      overflow: hidden;
    }

    /* Style the summary (button) */
    summary {
      padding: 20px;
      font-size: 4.5rem;
      font-weight: bold;
      cursor: pointer;
      background-color: #0d47a1;
      color: #ffffff;
      display: flex;
      justify-content: space-between;
      align-items: center;
      user-select: none;
    }

    /* Add a chevron icon */
    summary::after {
      content: '\25BC';
      font-size: 1em;
      transition: transform 0.3s ease;
    }

    details[open] summary::after {
      transform: rotate(180deg);
    }

    /* Panel content */
    .panel {
      padding: 20px;
      background-color: #212121;
      border-top: 1px solid #333;
    }

    /* Buttons */
    .remote-btn {
      width: 100%;
      padding: 25px;
      font-size: 3.5rem;
      border: none;
      border-radius: 6px;
      background: #333;
      color: #fff;
      cursor: pointer;
      margin-bottom: 10px;
      transition: background 0.2s ease, transform 0.1s ease;
    }

    .remote-btn:hover {
      background: #444;
    }

    .remote-btn:active {
      transform: scale(0.98);
    }

    /* Color accents */
    .pdf { background: #6f42c1; }
    .emailprep { background: #009688; }
    .purge { background: #e63946; }

    .pdf:hover { background: #7f56d9; }
    .emailprep:hover { background: #00796b; }
    .purge:hover { background: #c53030; }

    #toast {
      position: fixed;
      bottom: 20px;
      right: 20px;
      background-color: #333;
      color: white;
      padding: 12px 20px;
      border-radius: 5px;
      display: none;
      z-index: 1000;
      font-size: 1.2rem;
    }

    /* Touch-friendly */
    @media (hover: none) {
      summary {
        padding: 25px;
      }
    }
  </style>
</head>

<body>
  <h1>PO PDF Tools</h1>
  <div class="accordion">

    <details>
      <summary>PDF Operations</summary>
      <div class="panel">

        <details>
          <summary>Renaming</summary>
          <div class="panel">
            <button class="remote-btn pdf" onclick="runFunction('renameTradingPartnerFiles')">Rename Trading Partner Packing Slips</button>
            <button class="remote-btn pdf" onclick="runFunction('renameShippingLabels')">Rename Shipping Labels</button>
            <button class="remote-btn pdf" onclick="runFunction('renameShippingManifestPDFs')">Rename Shipping Manifests</button>
            <button class="remote-btn pdf" onclick="runFunction('renameFileToPDF')">Rename Files With No Extension to PDF</button>
            <button class="remote-btn pdf" onclick="runFunction('renameDDPOSFiles')">Rename DDPOS Files</button>
          </div>
        </details>
        
        <details>
          <summary>Friday Operations</summary>
            <div class="panel">
              <details>
                <summary>Renaming (uses -thurs functions)</summary>
                  <div class="panel">
                    <button class="remote-btn pdf" onclick="runFunction('renameTradingPartnerFilesThurs')">Rename Trading Partner Packing Slips (-thurs)</button>
                    <button class="remote-btn pdf" onclick="runFunction('renameShippingLabelsThurs')">Rename Shipping Labels (-thurs)</button>
                  </div>
              </details>
            </div>
        </details>

        <details>
          <summary>Copying</summary>
          <div class="panel">
            <button class="remote-btn pdf" onclick="runFunction('copyTradingPartnerPDFs')">Copy Trading Partner Packing Slips</button>
            <button class="remote-btn pdf" onclick="runFunction('copyShippingLabelPDFs')">Copy Shipping Labels</button>
            <button class="remote-btn pdf" onclick="runFunction('copyShippingManifestPDFs')">Copy Shipping Manifests</button>
          </div>
        </details>

        <details>
          <summary>Prepping</summary>
          <div class="panel">
            <button class="remote-btn pdf" onclick="runFunction('prepareShippingDocuments')">Prepare Shipping Documents</button>
            <button class="remote-btn emailprep" onclick="runFunction('emailPrep')">Run Email Prep</button>
          </div>
        </details>

      </div>
    </details>

    <details>
      <summary>Purge Operations</summary>
      <div class="panel">
        <button class="remote-btn purge" onclick="runFunction('purgeDailyShippingDocs')">Purge Daily Shipping Docs</button>
        <button class="remote-btn purge" onclick="runFunction('purgeAllPDFs')">Purge All PDFs</button>
      </div>
    </details>

  </div>

  <div id="toast"></div>

  <script>
    function showToast(message) {
      const toast = document.getElementById('toast');
      toast.textContent = message;
      toast.style.display = 'block';
      setTimeout(() => { toast.style.display = 'none'; }, 3000);
    }

    function runFunction(funcName) {
      showToast(`Running ${funcName}...`);
      google.script.run
        .withSuccessHandler(result => showToast(result))
        .withFailureHandler(error => showToast(`Error in ${funcName}: ${error.message}`))
        [funcName]();
    }
  </script>
</body>
</html>
